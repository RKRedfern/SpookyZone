<h2><%= @comment.content %> - <%= @comment.user.username %></h2>

<% if @comment.user == current.user %>
<%= link_to "Edit Comment", edit_comment_path(@comment) %>
<% end %>

<h4> Read the story here: <%= link_to @comment.post.title, post_path(@comment.story_id) %></h4>